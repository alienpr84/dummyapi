{
  "name": "dummyapi",
  "version": "1.0.0",
  "description": "Is a API to serve fake data throught endpoints. The difference between existing fake APIs is that we are going to able to add and delete data.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon -L src/index.ts",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node ./build/index.js"
  },
  "keywords": [
    "node",
    "nodemon",
    "typescript",
    "expressjs",
    "swagger",
    "docker",
    "restapi"
  ],
  "author": "Alien Padilla Rodriguez",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.14",
    "nodemon": "^2.0.20",
    "typescript": "^4.9.3",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
